# Execute this project within six hours

You are the implementation lead, experimenter, skeptical reviewer, and publication engineer for the attached project. Work autonomously within the declared permissions. Do not replace the research question or turn the work into sandbox exploitation. Read AGENTS.md, START_HERE.md, protocol/EXPERIMENT_SPEC.md, protocol/THEORY_TO_EXPERIMENT.md, the current paper, and its evidence ledger before editing.

## Goal
Deliver a reproducible evaluation of when a detectable local simulator still supports a behavioral claim, and when missing decision-relevant prerequisites or violated audit assumptions invalidate that claim. The required output is a complete English report using the supplied Apart Research Word template, with exactly eight main-text pages excluding references and appendices, plus code, raw records, analyses, figures, and a release ZIP.

## First actions
1. Run `python -m unittest discover -s tests -v` and `python scripts/run_local.py --out results/reproduced`.
2. Recheck both inherited v5 certificate files with the independent checker commands in START_HERE.md. Do not infer success from an optimizer status.
3. Inspect the current report and exact template. Verify the supplied manuscript is eight main pages after rendering. References must start on page nine. The PDF and editable DOCX must correspond to the same source.
4. Check only the presence of explicitly configured credentials and an authorized inference endpoint. Do not expose secret values or search unrelated accounts. If model IDs, current prices, or a spending authorization are missing, record BLOCKERS.md, complete E1 scripted/E2/E3 and ship the honest no-LLM report. Do not block the whole project.

## Conditional model experiment
The scaffold already implements local tools, strict JSON parsing, causal timing, full logs, conservative cost reservations, request/time ceilings, and two inference transports. Real provider behavior has not been tested. Validate the transport using `--mock`, then at most a small separately logged provider pilot. A pilot may fix schema/transport or choose adequate output limits, but must not optimize the desired behavioral finding. Freeze a new configuration and source manifest after any pilot change. Use distinct pilot seeds/output directories.

Prefer two already accessible model snapshots with different capability/cost profiles, but one complete model is better than two selectively incomplete models. Do not guess model availability or current prices. Record exact returned model IDs, generation parameters, token usage, and public price source. The default core is 240 episodes per model, 480 for two models, and at most 2,592 requests absent retries. The hard request ceiling is 4,000. Do not exceed the user-authorized dollar cap. Local counters are per output directory: subtract all pilot and prior expenditure before configuring a new directory, and report combined spending. A new output directory never grants a new budget. Stop new inference by hour four, preserving time for analysis and release. The default inference window is 9,000 seconds after a 90-minute setup; reduce it to the remaining workflow allowance if setup takes longer.

Run `python scripts/run_llm.py --config configs/authorized.json --out results/llm_confirmatory`. The script does not execute model-proposed code. No safety classifier is disabled. Default transport retries are zero; any added retry policy must be frozen before confirmatory runs and retain failures. Do not retry behavior to improve scores. Do not use your project-aware Codex conversation as an allegedly blinded evaluated agent. Independent model calls get only the defined model-facing input, never this repository, hidden labels, analysis code, or future diagnostic outcomes.

## Analysis
Use scripts/analyze_llm.py after real runs. Report planned episodes, episodes with an attempted provider request, completed actions, complete probability reports, refusals, malformed JSON, truncations, timeouts, budget stops, and unstarted episodes. Keep completed actions even when a probability report fails. Compute per-outcome denominators and missingness bounds. For classification use frozen orientation and half credit at 0.5. Use the prescribed paired label-randomization test with Holm adjustment for the four planned primary model/family comparisons. Report effect intervals descriptively. Do not treat a nonsignificant effect as equivalence or a probability report as latent belief.

Keep E2's known-kernel reweighting and E3's distributional certificates separate from live LLM claims. Do not apply scripted-policy scores to an LLM whose membership in that formal class has not been justified. Demonstrate the predicted separation even when LLMs never take the harmless shortcut; this is a valid bounded result, not a reason to introduce stronger pressure or hidden goals.

## Report and release
Update paper content from saved results, not memory. Use paper/submission_blocks.json and scripts/build_report.py as the editable path. All main report sections follow the supplied template: Introduction; Related Work; Methods; Results; Discussion and Limitations; Conclusion; Code and Data. Include the required Limitations and Dual-Use Considerations appendix and truthful LLM Usage Statement. Retain exactly four one-month extensions, listed in the current manuscript. Do not invent authors, affiliations, repository URLs, or confirmations.

Build the DOCX, render it through LibreOffice, and run scripts/verify_report.py. Inspect every PDF page as an image. References must start on page nine. No missing glyphs, formula overlap, cropped tables, or empty padding pages. The abstract must remain exactly 150 words. A longer appendix is permitted; hidden critical assumptions in appendices are not. Retain a portable Markdown version and all native sources. Font files must never be bundled.

Deliver: `submission_report.pdf`, `submission_report.docx`, `submission_report.md`, `EXECUTION_REPORT.md`, raw request/episode ledgers, results and figure data, `verification/final_checks.json`, `MANIFEST.json`, and a ZIP. Explain what actually ran and what remains blocked. Retain every negative result and deviation. Do not submit, email, or publish automatically. Independent human theorem review, source review, authorship approval, and any public disclosure remain author gates.
