# Execution deviations and provenance

This log concerns the 14 September 2026 reproduction. The inherited development history remains in `provenance/inherited_v6/EXECUTION_REPORT.md`. No observed scientific outcome was replaced or selected to obtain a desired sign.

1. **Conditional inference was not activated.** Exact model identifiers, endpoint authorization, a spending budget, pricing, and the configured credential are absent. Following the assignment's explicit fallback, E1 scripted controls, E2, and E3 were reproduced without a provider pilot or confirmatory inference. This is an authorization blocker, not a negative model result.
2. **Existing CPU evidence was reproduced before changes.** The prescribed seven output files in `results/reproduced/` match the supplied CPU files byte for byte. Repeated outputs do not add independent observations. Both inherited certificate files were checked independently before edits.
3. **Failure accounting was corrected after the initial mock freeze.** The inherited provider adapter discarded visible refusal/truncation metadata and usage in some incomplete responses. It also substituted a requested identifier when no returned model identifier was available. The revised adapter retains permitted public outcome fields, records missing returned IDs as missing, classifies explicit refusals/truncations/timeouts, and preserves completed actions when reports fail. Hidden reasoning fields are excluded. Strict JSON parsing now rejects non-finite constants and duplicate keys. No real provider response was available or changed.
4. **The analyzer now accounts for the entire frozen design.** It reports attempted and planned denominators, separate probability-report missingness, absent ledger records, world-specific behavior bounds, failure categories, and combined reservations/charges. Holm adjustment reserves all four planned comparisons when fewer models are available. Existing scientific estimates, prompts, mechanisms, seeds, and model configuration were not tuned.
5. **Mock cohorts are distinct.** `results/mock_new/` contains the initial 12-episode transport reproduction. `results/mock_verified/` contains a fresh 12-episode run after the transport/analysis corrections, with its own source/configuration freeze. `verification/mock_transport_final/` is inherited evidence. Later publication and packaging files do not retroactively change either freeze. No existing mock directory was resumed under a changed source configuration.
6. **The CPU runner now rejects nonempty output directories.** A deliberate rerun against `results/reproduced/` was rejected without modifying evidence. Future reproduction requires a fresh directory. The scientific computation is unchanged.
7. **Current provenance replaces inherited review claims.** The report and claim ledger distinguish reproduction from new experiments, mark the inherited author name for confirmation, and identify unavailable primary attachments [1,3]. Literature metadata was not re-fetched under the network restriction. No independent human source, theorem, or authorship review is claimed.
8. **Publication tooling was absent at startup.** The required Python libraries, LibreOffice, Pandoc, and Old Standard font were missing. A narrowly scoped exception for isolated dependency downloads was requested; no such download is authorized merely by a prospective inference plan. Final build and visual verification must record the actual outcome of this dependency gate.

No provider retries, additional model selection, prompt optimization, real target interaction, external audit, submission, or publication occurred.
