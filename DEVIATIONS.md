# v7 changes relative to the received Codex working release

The received change log is retained at `provenance/codex_received/DEVIATIONS.md`. That log, and the older baseline, must not be treated as current execution claims.

| Change | Reason | Effect on evidence |
|---|---|---|
| Reproduced seven CPU files in a fresh directory | Verify the return before relying on it | Byte-identical; no additional observations |
| Added finite-float JSON parsing and a regression test | Numeric exponent overflow bypassed the prior constant filter | Future parser change; no recorded output rewritten |
| Restored source locations for references [1,3] | The original attachments are available here | Removes a workspace-specific source gap; exact-PDF metadata caveats remain |
| Added partial-identification prior work [12] | Narrow novelty against relevant precedent | No theorem or numerical change |
| Regenerated PDF/DOCX/Markdown | Received rendered documents were stale | Current source now has actual rendering and QA |
| Removed a duplicate reproduction paragraph | Actual rendering overflowed the 8-page main-text requirement | Same information retained elsewhere; no type/margin shrink |
| Replaced malformed inline supremum with equivalent words | Math renderer produced the wrong visual form | Definition unchanged |
| Added local build/inference preflight and local-font registration helper | Prevent another late dependency blocker | No downloads, secrets, provider calls or model observations |
| Added a focused next-stage Codex instruction | The remaining empirical gap is authorized real inference | No permission assumed or model chosen |

The experimental prompts, world mechanisms, target policy, CPU seed, 480-episode prospective design and raw evidence remain unchanged. No real provider pilot was run. The preliminary editorial build failures and their fixes are recorded rather than erased. The final checks apply to the newly rendered v7 report, not inherited binaries.


# v8 continuation — separate official-client pilot

The v7 section above describes historical work, not this continuation. Its report and source are preserved in provenance/subscription_v8_before_revision/.

| Change or deviation | Reason and evidence consequence |
|---|---|
| Separate codex-subscription-pilot-v8 arm | User authorized existing ChatGPT access; model-plus-Codex-client results are not pooled with API or mock cohorts |
| New route-specific readiness and official App Server adapter | The inherited responses/chat adapter cannot run subscription authentication; its API implementation remains unchanged |
| Human billing-control attestation | Passive zero-credit telemetry did not expose an included-only request lock; the user confirmed account controls before calls; cost/credits remain null |
| Fresh process/thread for each response and serialized public history | Official-client delivery differs from API roles and retained per-episode threads; frozen prospectively |
| Profile selection and skill-disable repairs before inference | Failed and intermediate no-turn audits retained; no target-model response preceded repair |
| Prospective deadline/analysis guards and one fixture correction | Initial failed targeted test log retained; final 40 subscription tests pass; no outcome-informed prompt tuning |
| Effective runtime model metadata absent in 102/102 turns | Requested/configured gpt-6-astra is recorded separately, never substituted |
| No complete native-tool or assembled-instruction inventory | Combined controls, startup catalogs, marker-denial checks and exact payload reconstruction support a bounded interface, not universal blindness |
| 102 turns used from 108 maximum | The subject used 42 of 48 available probes; all 20 episodes completed with no replacement or exclusions |
| All commitments standard | Retained as an observed outcome; no incentive or prompt changes followed compliance |
| Working-source revision instead of rendered v8 report | Current dependencies/font unavailable at preflight; inherited binaries preserved, new source explicitly unrendered |
| Audit-created bytecode caches | Three snapshot caches are excluded from packaging; all 45 source hashes still match |

The pilot source/configuration/analysis freeze was unchanged during inference. No post-outcome repair or second cohort was run. The two-model API plan and four-comparison correction family remain prospective/intact. Root current checks no longer claim the revised v8 source has passed rendering. No model, scientific or submission approval is inferred from package integrity.
