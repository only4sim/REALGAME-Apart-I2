# Historical access and subject boundary

These sanitized records document the completed v8 subscription pilot. They preserve official-client capability/model discovery, the user's exact limited approval, the subsequent account-control attestation, prospective protocol amendment, failed/intermediate boundary checks and final controls.

They contain no credential contents or unfiltered reasoning streams. Metadata discovery can require network access; it is not part of offline reproduction. The helper scripts here are retained for explicit future capability work, not invoked by the default workflow.

The approval was limited to the completed pilot. Its pre-call decision/configuration wording is historical and is not permission for a new experiment. Do not infer child-process access, current quota or billing controls from these saved files. Keep API and ChatGPT-client routes separate, and obtain a new explicit scope before future inference.

Read [the protocol amendment](PROTOCOL_AMENDMENT.md), [the completed pilot report](../docs/PILOT_RUN_REPORT.md), and [the reproduction guide](../docs/REPRODUCING.md). Exact evaluated copies are also retained in the pilot's source snapshot.
