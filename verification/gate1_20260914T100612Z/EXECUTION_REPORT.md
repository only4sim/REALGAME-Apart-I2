# Gate 1 execution record

Checked at 2026-09-14T10:06:12.707894+00:00.

All 18 unit tests passed, including exponent-overflow rejection. The default readiness check completed and correctly reported inference blocked. No real models were run, no model prompts were transmitted, and combined pilot/main spending for this continuation is USD 0. No authorized configuration or frozen real-model cohort was created. Missing authorization is not a model failure or a negative awareness result.

The human must provide exact model IDs, explicit endpoint/provider approval, permission to transmit the protocol's synthetic prompts and histories, and a single global pilot-plus-main spending ceiling. Hosted inference additionally requires numeric input/output token rates with a dated primary source and the named credential in the process environment. The current defaults use provider responses and https://api.openai.com/v1; these are not human authorization. OPENAI_API_KEY is absent. Only its presence was checked; no secret value was recorded.

The current host cannot build the report: python-docx, lxml, PyMuPDF and matplotlib are unavailable to the configured Python interpreter; pandoc and LibreOffice are unavailable on PATH; fontconfig resolves Old Standard TT to DejaVu Sans. The existing-font helper found no font at its default local location. No downloads or dependency installations were attempted under the project's network restriction.

The existing PDF, DOCX, manuscript blocks, Markdown and original template match the inherited final-check hashes. The existing archive passed CRC and every member's manifest hash; all 441 manifest-listed workspace files match it. This is current integrity verification of the earlier rendered release, not a new rendering or independent visual review. The inherited publication checks document eight main pages, references beginning on page nine, thirteen total pages and 150 abstract words.

Gate 1 explicitly requires preserving the rendered report when authorization is absent. No manuscript, source code, raw evidence, historical check, root execution report or existing ZIP was changed. This directory holds only the new local preflight record and zero-call spending statement. No substitute CPU experiment or saved mock cohort was run. The existing ZIP excludes these new preflight files and retains its historical execution report.

Model compatibility, returned model IDs, real-model classification/calibration, probe usage and shortcut behavior remain unmeasured. The historical evidence remains 960 scripted episodes and 36 saved mock episodes, with zero evaluated LLM episodes. The four planned model/family comparisons and the research question were not changed.

Human author/affiliation confirmation, scientific review, disclosure approval, artifact location and venue eligibility remain separate submission gates. Nothing was submitted or published.
