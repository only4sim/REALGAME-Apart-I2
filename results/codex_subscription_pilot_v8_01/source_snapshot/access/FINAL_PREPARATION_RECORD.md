# Final preparation record — before experimental outcomes

Recorded on 2026-09-14. The original 18 tests and 37 initial subscription tests passed together (55 tests). A later prospective review added monotonic-deadline guards, including buffered event processing, and rejected orphan/duplicate submitted-turn analysis records. The first targeted 40-test run exposed one incomplete MOCK-NOT-LLM test fixture; the fixture was corrected without changing the subject prompts. The final targeted run passed all 40 subscription tests. Thus 58 distinct tests have passing evidence across the retained logs, not 95 distinct tests or scientific observations.

The no-turn transport preflight preceded the final timer changes. Boundary logic and controls were unchanged; the execution freeze records the final source hashes. No target-model turn preceded these changes. The unexecuted provisional design-only preparation helper was removed because the official subject runner now exists.

Earlier benign local sandbox diagnostics also included an unsupported old subcommand form and an unavailable Python executable within the minimal permission profile. The corrected shell-builtin marker checks succeeded. These were local command diagnostics, not experimental model outcomes.

The runner reserves a global turn allowance before writing the frame, then separately records a fully submitted frame. Reservations may conservatively exceed confirmed submissions after a failed write. Runtime model IDs and unavailable costs remain missing. No source, prompt, model, effort or schedule changes are planned during inference.
